#sample cicd practice file 2
name: Hello World Workflow
on: 
    pull_request:
        branches:
            - master
jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: Greet the world
              run: echo "Hello, WORLD!"